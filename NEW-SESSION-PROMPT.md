# Стартовый промпт для новой сессии (скопировать целиком в первое сообщение, приложить baustern-site-v9_2.zip + baustern-handoff-core.zip + baustern-handoff-film-v6.zip)

> НОВЕЙШЕЕ СОСТОЯНИЕ 15.09.2026 (Cowork, без bash — НЕ протестировано, НЕ задеплоено): читать `HANDOFF.md §10`. Владелец отверг мобильный
> immersive-релиз 14.09 по скринам с iPhone (пульт «дешевит», нет ощущения скролла и перескок комнат, чипы вместо подсказок, урезанные
> кадры, неинформативные пролёты). Ответ — не заплатка, а новый движок «вертикальный кат» за флагом: `site/js/film-mobile-v2.js` +
> `site/css/mobile-film-v2.css` (скролл-скраб как на десктопе, нативный скролл без лока, ноль пульта, пины на фото + bottom-sheet,
> пан-скан широких кадров), включается `?mv2=1` или `FILM.mobileV2.enabled`; v1 остаётся как откат. Контракт портретных ассетов и сцена
> входа «Ankunft» подготовлены в `data.js` (`FILM.portrait`, `FILM.ankunft`, оба `enabled:false`) и `core/sources/sprintD/PROMPTS-v2.md`.
> ПЕРВОЕ ДЕЙСТВИЕ: `cd core/dev && MV2=1 W=375 node jsdom-smoke.js`, затем `?mv2=1` в DevTools-эмуляции и на реальном iPhone
> (порядок в HANDOFF §10.4). Ничего из v2 не включать в прод без скриншотов владельца. Дальше: генерация по PROMPTS-v2 (A→B→C),
> `FILM.portrait.enabled`, `FILM.ankunft.enabled`, Ankunft на десктопе по §10.3.

> НОВЕЙШЕЕ СОСТОЯНИЕ 14.09.2026, 14:36 UTC: первым читать `IMMERSIVE-MOBILE-2026-09-14.md`. Production **`6aa8063e94dbca6b73db1782`** на https://www.baustern.ch. Пользователь отверг «отдельный плеер 16:9 + информация снизу» из предыдущего релиза. Теперь mobile — единая полноэкранная сцена с наложенными текстом/действиями, горизонтальным осмотром широкого стоп-кадра и полным просмотром по иконке. На узком экране есть кадрирование; не обещать одновременно полный широкий кадр без обрезки. Сохранены исходные видео, автоматическая материализация, удержание кадра и компактные разделы. Исправлены выход из последней комнаты и сенсорная активация: pointerup + click dedupe, с отдельным сбросом на новом pointerdown мыши (WebKit совместимый click нельзя отличать только по pointerType). Код опубликован после исправленного draft; первый draft с двойным переходом WebKit в prod не попадал. Физическая приёмка нового вида на iPhone 15 ещё нужна. Не возвращать отдельное окно, белые панели, звук или мобильный ползунок. CRM и её backup/юридические вопросы в этой UI-задаче не менялись; новые реальные отправки запрещены без нового разрешения.

> НОВЕЙШЕЕ СОСТОЯНИЕ 14.09.2026, 13:43 UTC: первым читать `SPATIAL-RESTORE-2026-09-14.md`. Production **`6aa7f9ee490a2ee08c6f22be`** на https://www.baustern.ch. Восстановлены пространственные desktop-подсказки и прозрачная dot-навигация, удалён весь звук. Мобильная версия показывает целые широкие кадры/клипы, затем держит стоп-кадр; семь комнат идут до компактных раскрывающихся информационных разделов. Rückblende автоматически материализуется за 6,2 с без ползунка. Есть пауза/повтор, просмотр полного изображения, доступные нативные диалоги; форма не скрыта в disclosure, её контраст исправлен. Код и пререндер опубликованы после двухдвижковых тестов. Физический iPhone 15 требует новой визуальной приёмки; универсальные 60 FPS не гарантировать. CRM/backup/юридические вопросы из предыдущего отчёта остаются отдельными; не делать новые реальные отправки без разрешения. Старые архивы, белые панели, звук и мобильный ползунок не восстанавливать.

> НОВЕЙШЕЕ СОСТОЯНИЕ 14.09.2026, 12:11 UTC: сначала `FRAME-BUDGET-2026-09-14.md`, затем `PRELAUNCH-CLOSURE-2026-09-14.md`, `DATA-RETENTION-REVIEW.md` и `CRM-BACKUP-READINESS-2026-09-14.md`. Production **`6aa7e431284ec329992390ec`** проверен на https://www.baustern.ch, Netlify Personal активен. Новые правки кадрового бюджета и ползунка уже опубликованы; изображения v13 не понижались. Все мобильные/desktop/форменные регрессии прошли; аппаратный Chrome p95 укладывается в 16,8 мс, SwiftShader всё ещё медленнее. Не обещать универсальные 60 FPS или физическую сертификацию iPhone. Бессрочное хранение CRM — решение владельца, не юридическое заключение; автоудаление не реализовывать по старому тексту о 12 месяцах. Требуются решение о backup/Supabase и юридическая приёмка. WEB-260914-010 и WEB-260914-011 уже проверены: не создавать новую реальную заявку без отдельного разрешения. Исторические архивы ниже не распаковывать поверх текущего проекта.

> НОВЕЙШЕЕ СОСТОЯНИЕ 13.09.2026 — `VISUAL-RELEASE-2026-09-13.md`. Читать его первым, затем CRM-отчёт 12.09. Код и новые versioned-ассеты v13 уже на Netlify (production `6aa6b9fd423cb601c55b54f3`); не восстанавливать `r1`, `m2`, `mat4` из исторических инструкций ниже. Исправлены наложения, подсказки, дублирование раковины и пропадание заголовков. Физические устройства и стабильные 60 fps программного desktop canvas не объявлялись подтверждёнными.

> НОВЕЙШЕЕ ПРОДОЛЖЕНИЕ 12.09.2026 — `CRM-60FPS-RELEASE-2026-09-12.md`. Читать первым. Уже работают реальная CRM с атомарной записью и вложениями, адаптивное 60-fps видео `m2p/` / `m2/`, материализация `mat4/`, PDF-каталог. Код интеграций и резервные копии в `core/integrations/`. Не откатывать это по историческому промпту ниже. Физические телефоны и авторизованная визуальная приёмка CRM остаются ограничениями, отражёнными в новом отчёте.

> АКТУАЛЬНОЕ СОСТОЯНИЕ, 12.09.2026: проект уже распакован в `C:\Users\prekr\Downloads\сайт ссс`. Не распаковывать архивы поверх него. Сначала прочитать `MOBILE-RELEASE-2026-09-12.md` и свежие отчёты `core/dev/reports/mobile-*`. Мобильный плеер — `site/js/film-mobile.js`, стили — `mobile-film.css` и `mobile-ui.css`, вертикальное видео — `m1p/`. Статический сервер — `node core/dev/static-srv.js`, build — `npm run build` из `core/dev/`. Ниже оставлен прежний стартовый промпт как история, а не текущий список незавершённых работ.

Ты — Principal Full-Stack / Creative Developer и Senior UI/UX Designer. Продолжаем проект BauStern (baustern.ch, Швейцария),
сайт-фильм «Die Wohnung ist die Seite» на vanilla JS + GSAP + Lenis, деплой на Netlify.

ПЕРВОЕ ДЕЙСТВИЕ: распакуй приложенные архивы (site → working/site, остальные → working/) и прочитай `HANDOFF.md` ЦЕЛИКОМ —
это единственный источник правды (карта файлов, архитектура движка, решения, открытые проблемы, тестовый протокол, план).
Ничего из него не переспрашивай. Затем подними dev-сервер и прогони smoke-тесты из §0 и §5 — всё должно быть зелёным до правок.

ПРОТОКОЛ РАБОТЫ:
- Автономно и без промежуточных одобрений; вопросы — только на реальных развилках (выбор варианта дизайна).
- Задачи — через TaskCreate/TaskUpdate, атомарно.
- Каждое изменение проверяется в реальном браузере (Playwright, Chromium из песочницы) на 375 / 768 / 1440; консоль и сеть чистые
  (внешние ERR_TUNNEL_* от прокси песочницы — не баг). Никогда не отправлять реальные лиды: n8n стабить (см. HANDOFF §1).
- Полёты тестировать через dev/fling.js (mouse.wheel Playwright слишком редкий для детекции быстрого жеста).
- После правок pages.js/data.js/css/index.html — `node _tools/prerender.js` из site/, затем упаковка: index.html в КОРНЕ zip.
- Стиль общения: по-русски, коротко, без воды, критика приветствуется. В конце — структурированный отчёт с метриками.
- Комментарии в коде — по-немецки (традиция проекта). Текст сайта — Schweizer Hochdeutsch (ss, CHF, Sie).

ТЕКУЩАЯ ЗАДАЧА (см. HANDOFF §9 «Ограничения / что дальше»):
1. Прогнать тестовый протокол §5 (Playwright) на v10 и починить всё красное; проверить полёты с `.w-vid.on`, шов интро, материализацию B+ (сцена Rückblende), Bad-параллакс, Flur-хореографию на 1440/768/375.
2. Заменить процедурные «до»/«пусто» в img/film/mat/ на AI-кадры (sprintA/PROMPTS.md §1, check-align.py), маски через sam-masks.py.
3. Перегнать мастер через Topaz/RIFE (`sprintB/pipeline.sh all`), добавить AV1.
4. `node _tools/prerender.js`, упаковка, деплой.
