# BauStern v16 — рабочий план (актуально на 16.09.2026, 04:00)

Этот файл заменяет `BRIEF-v16.md` как единственный источник правды. Вставь его первым сообщением в новый чат.
Роль ассистента: эксперт по сайтам (фронт + бэк) и дизайнер с чувством вкуса. Стиль: прямо, без воды, по делу.

---

## 0. Какой моделью и с каким усилием делать каждый этап

| Этап | Модель | Усилие | Почему |
|---|---|---|---|
| 2. Движок B ч.1 (видео-пролёты) | **Opus 5** | **высокое** | Большой, но полностью описанный рефакторинг: машина состояний, удаление скраба/Lenis/snap. Нужна аккуратность, не креатив. |
| 4. 2.5D-холды (WebGL + depth) | **Opus 5** | **высокое** | Шейдер + пайплайн карт глубины, много технических развилок (см. §3.4). |
| 5. «Один вопрос — один жест» | **Fable 5.1** | **экстремальное** | Здесь придумывают, а не исполняют: механики, motion, что показать клиенту. |
| 7. Дизайн-проход 5 скилами | **Fable 5.1** | **высокое** | Вкус, палитра, типографика, вычистка «генерик»-паттернов. |
| 8. Финальные тесты + документация | **Opus 5** | **среднее** | Прогон Playwright/Lighthouse, отчёт, HANDOFF. |
| Бэк (Supabase + Telegram) | **Opus 5** | **среднее** | Обычная интеграция через Netlify Functions. |
| Мелкая косметика, тексты, правки копи | **Sonnet 5** | **низкое** | Не жечь дорогие модели на однострочники. |

Правило: **Fable — только на этапы 5 и 7.** Всё остальное Opus/Sonnet, иначе лимиты Fable закончатся до самого интересного.

---

## 1. Проект и правила

- Папка: `C:\Users\prekr\Downloads\сайт ссс`, деплой-папка `site/`. Netlify `leafy-bublanina-48b840`, site_id `225f9043-7ff5-4dc2-a1ed-15f71a999079`, прод `https://www.baustern.ch`.
- Стек: vanilla JS + GSAP (+ Lenis — убрать на этапе 2), без сборки. Статика на Netlify.
- **Правила владельца (Сержио):** покупки, смена тарифов, удаление ценных данных и публикация в прод — только с явного согласия. Деплой по умолчанию = draft. `core/_incoming/` (сырьё + личные фото) НИКОГДА не попадает в `site/`. Не утверждать, что проверил, если не проверял.
- Документация: `HANDOFF.md` (§11 — v15.x, §12 — v16 этап 3), `core/sources/sprintE/REGISTRY.md` (реестр всех ассетов и промптов, разделы «v15.1», «v15.2», «v16»).

---

## 2. Что уже сделано

**Этап 1 — быстрые фиксы ✅** (другой чат): меню Lösungen, `translate="no"` на бренд-термины, логотип в шапке (`site/img/logo.png`, `logo-word.png`), цветовые токены, телефон исправлен на `+41 76 524 98 98` везде.

**Этап 3 — ассеты v16 ✅** (см. `HANDOFF.md` §12, `REGISTRY.md` «v16»):
- 12 стиллов с видимыми проёмами (Schwelle, Küche, Bad, Schlaf, Wohnen + Rohbau; 16:9 и 9:16, 2K).
- 12 переходов Veo 3.1 Quality «через дверь/проём», 1080p 24 fps 8 с, каждый проверен покадрово.
- Веб-деривативы: `core/dev/v16-build.js` → `site/img/film/v16/` (см. §4).

**Этап 6 — секции ниже фильма ✅** (другой чат): Referenzen → Richtpreise (CHF) → Ablauf → Kundenstimmen → FAQ → Team/Nachweise → Offerte-формуляр, обещание «Offerte innert 48 h», sticky «Rückruf in 5 Min».

**Косметика ✅ (16.09, этот чат):** кнопка отправки формы переименована `Anfrage senden` → `Offerte anfragen` в четырёх местах: `site/js/app.js` строка 592 (label для `bindDraft`), `app.js` строка 655 (сброс после ошибки отправки), `site/index.html` и `site/kontakt.html` (пререндер). В `pages.js` она уже была правильной — поэтому в SPA всё выглядело верно, а на «холодной» странице нет.
→ **Правка ещё не задеплоена.** Первым делом в новом чате: draft-деплой и проверка кнопки на живой странице `/kontakt`.

---

## 3. Что осталось

### 3.1 Этап 2 — Движок B, часть 1: видео-пролёты ✅ СДЕЛАНО (16.09)
Реализовано в `site/js/film-v16.js` + `site/css/film-v16.css` + `FILM_V16` в `data.js`. Подробности и результаты проверок — `HANDOFF.md` §13.

### 3.2 Этап 4 — 2.5D-холды ✅ СДЕЛАНО (16.09)
16 карт глубины (`site/img/film/v16/depth/<room>-<L|P>.webp`, 240 КБ всего) + `site/js/depth-v16.js`. Подробности — `HANDOFF.md` §13.

### 3.2b v16b — «один дом»: согласованная цепочка ✅ СДЕЛАНО (16.09, в проде)
Морфинг между комнатами убран в корне: сначала свели цепочку стиллов (в проёме кадра N видна реальная комната N+1),
только потом сняли 6 новых пролётов Veo Quality. Портрет — кадрирование из ландшафта, без второй генерации.
Стыки клип↔холд 41,5–75 dB. Подробности и промпты — `HANDOFF.md` §14, `REGISTRY.md` «v16b».

<details><summary>Исходное задание (для истории)</summary>

### Этап 2 — Движок B, часть 1: видео-пролёты (Opus 5, высокое)
Суть: фильм перестаёт зависеть от положения скролла.
- Контроллер намерений: колесо / свайп / стрелки / клик по точкам навигации = команда «следующая или предыдущая глава», дебаунс 600–800 мс. Пока «в доме» — страница запинена.
- Пролёт = обычный `<video>` (`v16/clips/<a>-<b>-<L|P>.fwd.mp4`), назад = `.rev.mp4`. Никакого `playbackRate=-1`.
- Машина состояний `HOLD(k)` / `FLY(k→k+1)` / `FLY(k+1→k)`; команда «назад» посреди полёта стартует реверс с позиции `dur − t`, без прыжка. Клипы соседей держать в памяти (blob/preload).
- Стоп-кадр обязателен на каждой комнате: пока нет нового жеста, стоит `v16/stills/<room>-<L|P>.jpg` (он же первый кадр следующего клипа — переход бесшовный).
- **Удалить:** Lenis, CSS scroll-snap на `<html>`, скраббер кадров, папки `site/img/film/{f15,f15h,f15l,fp15}` (~150 МБ), гибридный автопилот. Ниже дома — нативный скролл.
- `FILM_V16` в `data.js`; `FILM_V15` оставить как откат `?film=15`.
- Ориентация: L на десктопе/ландшафте, P на телефоне (по `innerHeight>innerWidth`).
- Вес: клипы по 6–12 МБ. Добавить лёгкий tier (crf 23, 720p) и грузить его при `saveData`/медленной сети.
- Критерий: любой переход вперёд/назад стартует мгновенно, играет 2–3 с, 0 JS-ошибок на всех вьюпортах.

### 3.2 Этап 4 — 2.5D-холды (Opus 5, высокое)
- Карта глубины для каждого холда (16 штук: 8 комнат × 2 ориентации). Развилка: (а) `transformers.js` + `Xenova/depth-anything-small-hf` прямо в Chrome через `browser_run_code_unsafe`/страницу-утилиту — не нужен Python; (б) Python + Depth Anything V2 локально, если bash-песочница починится. Начинать с (а).
- WebGL-квад: смещение UV по глубине на вектор от мыши (ПК) или drag/гироскоп (телефон, iOS требует тап-разрешение — по умолчанию drag). Амплитуда ±1.5 % кадра, без инпейнтинга.
- Плюс дрейф тумана за окном (маска по «далеко») и дыхание света 8-секундным циклом.
- Фолбэк для слабых устройств: CSS-параллакс из 2 слоёв. Бюджет ~1 мс/кадр.
</details>

### 3.3 Этап 5 — «Один вопрос — один жест» (Fable 5.1, экстремальное)
Принцип: не досье, а диалог. В каждой комнате ОДИН доказательный элемент, без нагромождения.
- Ankunft: обещание + одна цифра.
- Schwelle («Кто отвечает?»): шесть пинов-Gewerke стягиваются линиями в точку «Ein Bauleiter».
- Küche («Сколько стоит?»): калькулятор живёт здесь — пин на столешнице → шторка с ползунками, сумма меняется крупной цифрой в кадре.
- Bad («Что за стеной?»): «рентген» — курсор/палец как фонарик, под ним проявляется схема труб (SVG под радиальной маской) + штамп «SIA-konform».
- Schlaf («Как долго?»): Ablauf-этапы световой линией вдоль стены.
- Wohnen + Rückblende: шторка до/после (только здесь).
- Eingang: ключи, подпись, форма-Akte.
- Вкладка «Akte» у правого края = прогресс «Anfrage → Schlüssel». На телефоне — bottom-sheet по тапу на пин, карточек в зоне фильма нет.
- Рефрен «Vier Versprechen»: по строке на Ankunft/Schwelle/Bad/Eingang, в Eingang собираются вместе.

### 3.4 Этап 7 — Дизайн-проход (Fable 5.1, высокое)
Цепочка скилов строго по порядку, не параллельно:
`redesign-existing-projects` (аудит, генерик-паттерны) → `nielsen-usability-audit` (проблемы с приоритетами) → `ui-ux-pro-max` (палитра/шрифты под Berghaus) → `high-end-visual-design` (правила для новых элементов) → `impeccable` (финальный проход по секциям).

### 3.5 Этап 8 — Финал (Opus 5, среднее)
Playwright: desktop 1440, phone 390, landscape 844×390, tablet 1024 — 0 JS-ошибок, 0 HTTP≥400. Lighthouse на телефоне (цель LCP < 2.5 с). Обновить `HANDOFF.md` §13 и `REGISTRY.md`. Прод — только по явному «в прод».

### 3.6 Бэк — позже (Opus 5, среднее)
Supabase-отзывы сейчас отдают 401 (нет рабочего anon-key) → кураторский фолбэк. План: лиды (формы + калькулятор) в одну таблицу + уведомление в Telegram через Netlify Functions, ключи не во фронте.

---

## 4. Ассеты v16 (готовы, лежат в репозитории)

Мастера (НЕ деплоить): `core/_incoming/v16/` — `t_<a>-<b>-<L|P>_1080p_*.mp4` (12 клипов), `<room>-<L|P>_2K_*.jpeg` (12 стиллов), `_rejected/` (забракованная первая версия Schwelle→Küche 9:16).

Веб (`site/img/film/v16/`), пересобирается `node core/dev/v16-build.js` (env `V16_ONLY=clips|stills`):
- `clips/<a>-<b>-<L|P>.fwd.mp4` — вперёд, h264 crf19 24 fps faststart, без звука.
- `clips/<a>-<b>-<L|P>.rev.mp4` — назад (ffmpeg reverse, бонусы не тратятся).
- `clips/<a>-<b>-<L|P>.first.jpg` / `.last.jpg` — постеры для бесшовного старта и стопа.
- `stills/<room>-<L|P>.jpg` (1920×1080 / 1080×1920) + `-1280.jpg` + `.webp`.

Пары комнат: `ankunft-schwelle`, `schwelle-kueche`, `kueche-bad`, `bad-schlaf`, `schlaf-wohnen`, `wohnen-eingang`. Rückblende — без клипа (шторка до/после на стоп-кадре Wohnen; Rohbau-стиллы есть).

Двери намеренно разные: Ankunft — «большая» дверь-событие; Schwelle→Küche, Küche→Bad, Bad→Schlaf, Wohnen→Eingang — дверь/проём; Schlaf→Wohnen — открытый проход без двери.

---

## 5. Инструменты и обходные пути (проверено 15–16.09)

- **bash-песочница сломана.** Node на ПК запускать через Playwright-MCP `browser_run_code_unsafe`:
  `const proc=page.constructor.constructor('return process')(); const cp=proc.mainModule.require('child_process');`
  Длинные задачи — `cp.spawn('cmd',['/c','...'],{detached:true,windowsHide:true,stdio:['ignore',fd,fd]}).unref()` и ждать циклами ≤55 с по логу (вызов дольше ~2 мин перезапускает MCP-сервер).
- ffmpeg: `core/dev/node_modules/ffmpeg-static/ffmpeg.exe`.
- Проверки: `core/dev/jsdom-smoke.js` (`MV2=1 W=375` / `MV2=0 W=1440`, `NODE_PATH=core/dev/node_modules`), `core/dev/prerender-run.js` (`CHROME=` Google Chrome), локальный сервер `node core/dev/static-srv.js` → `http://localhost:8099`.
- Деплой: `npx -y netlify-cli@17 deploy --dir=site --message "..."` (draft). Без TTY ничего не печатает — статус смотреть: `npx -y netlify-cli@17 api listSiteDeploys --data "{\"site_id\":\"225f9043-7ff5-4dc2-a1ed-15f71a999079\",\"per_page\":3}"`.
- **Google Flow** (если понадобится догенерация): картинки Nano Banana Pro бесплатны, видео Veo 3.1 Quality = 100 бонусов. Сгенерированные картинки НЕ появляются в пикере «Первый/Последний» — их надо загрузить как файлы (draft-деплой временной папки → `fetch` → перехват `input[type=file]` → `DataTransfer`). Скачивание: клик «Скачать медиаконтент» → «1080p» (первый клик запускает апскейл, второй скачивает); повторные автозагрузки в одной вкладке Chrome блокирует → по одной на свежую вкладку. При смене аккаунтов меняются индексы `/u/N/` — актуальный искать через `accounts.google.com/SignOutOptions`.
- Аккаунты Flow: makafon ashh (проект `743c04fd-…`, ~50 бонусов), Дима Пупкин (проект `a0a0e0dd-…`, остаток — резерв на переделки).

---

## 6. Бренд и палитра «Berghaus» (утверждено)

Навий `#0D1230` (UI-поверхности), фирменный синий `#1A64C8` (только действие: логотип, главный CTA, ссылки), латунь `#B8925A` (только внутри фильма: пины, цифры, документ), бетон `#8A8680`, туман `#9FB3BF`, бумага `#F3F0EB`. Один акцент на весь сайт, бирюзовую кнопку не возвращать.
Логотип: `site/img/logo.png` + `logo-word.png`, оригинал `core/sources/brand/baustern-logo-original.jpg`. Желательно (не срочно): перерисовать в SVG + фавикон/OG-image.

---

## 7. Открытые вопросы владельцу (не блокируют этап 2)

1. Реальные цифры для Bauakte (цена/сроки/гарантия по комнатам) и один настоящий документ — пока плейсхолдеры с пометкой «Beispiel».
2. Фото реальных объектов для Referenzen (район, срок, сумма) — прислать в чат.
3. Handelsregister-номер и страховка для блока доверия у формы.
4. Часы работы для sticky-кнопки (сейчас без часов, только «Rückruf in 5 Min»).
5. Мелочь: в карточке «Vier Versprechen» кнопка-ссылка на контакт всё ещё называется `Anfrage senden` (`pages.js` ~750 и `404.html` ~98). Это не кнопка формы — менять на «Offerte anfragen» или оставить? Одна правка в двух местах.

---

## 8. Критерии готовности v16

0 JS-ошибок и 0 HTTP≥400 на desktop/phone/landscape/tablet; любой переход вперёд/назад ≤ 3 с со мгновенным стартом; на телефоне нет карточек в зоне фильма; один акцентный цвет; LCP на телефоне < 2.5 с; `core/_incoming` вне `site/`; прод — только по явному «в прод».
